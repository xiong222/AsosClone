import styles from './index.css';
import React from 'react';

const CategoryHeader = () => (
  <section id="category-banner-wrapper" className={styles.wrapper}>
    <h1 className={styles.categoryTitle}>Men's New in: Clothing</h1>
  </section>
);

export default CategoryHeader;
