import connect from 'react-redux/es/connect/connect';
import LargeScreenSort from './LargeScreenSort';

const mapStateToProps = (state) => ({});

export default connect(mapStateToProps)(LargeScreenSort);
