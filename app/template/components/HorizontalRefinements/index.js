import connect from 'react-redux/es/connect/connect';
import HorizontalRefinements from './HorizontalRefinements';

const mapStateToProps = (state) => ({});

export default connect(mapStateToProps)(HorizontalRefinements);
